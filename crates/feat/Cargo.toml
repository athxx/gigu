[package]
name = "feat"
version = "0.1.0"
edition = "2024"

[dependencies]
domain = { path = "../domain" }
infra = { path = "../infra" }
ui = { path = "../ui" }
makepad-widgets = { git = "https://github.com/makepad/makepad.git", branch = "dev", features = ["maps"] }
