[package]
name = "feat"
version = "0.1.0"
edition = "2024"

[dependencies]
domain = { path = "../domain" }
infra = { path = "../infra" }
ui = { path = "../ui" }
makepad-widgets = { path = "../../../makepad/widgets", features = ["maps"] }
